{% extends "base.html.twig" %}
{% set menu_name = "account_management" %}
{% set admin_role = "ROLE_ADMIN" %}
{% set standar_user_role = "ROLE_USER" %}
{% set trainer_role = "ROLE_TRAINER" %}
{% use "menu_admin_home_page.html.twig" %}
{% block content %}
        <div class="background" id="background">
                {% include "flash_messages.html.twig" %}
        <div class="main_content">
          <div class="center_content">
            <div class="grid_panel">
              <div class="user_trainings_list">
                <div class="d-flex pt-3 pr-3" style="">
                  <a href="{{ path("account_management")}}" class="btn-sm btn-dark btn-block add_plan_btn text-center" role="button"><i class="fas fa-backward"></i>  Powrót na listę</a>
                </div>
                <div class="row text-center pt-4 pl-4 pr-4 pb-4">
                  <h2 class="ml-5">Tu znajdują się wybrane elementy <span class="text-success">strony głównej</span> którymi możesz zarządzać</h2>
                </div>
            <div class="grid_panel_center">
              <div class="user_profile">
                <div class="text-center mt-5">
                <div class="text-center mt-2">
                  <h3 class="mt-2">Strona główna:</h3>
                  <hr class="line mb-2">
                    {{ form_start(formPageElement, {'method': 'POST', 'attr': {'class': 'text-center'}})}}
                      {{ form_widget(formPageElement, {'attr': {'class': 'custom_inputs_add_form'}})}}
                      {{ form_errors(formPageElement) }}
                    {{ form_end(formPageElement) }}
                  <hr class="line">
            </div>
            </div>
          </div>
        </div>
        </div>
        </div>
        </div>
        </div>
{% endblock %}